module.exports = {
  future: {
    webpack5: true,
  },
  // webpack: (config, { isServer }) => {
  //   if (isServer) {
  //     require('./scripts/generate-images') // eslint-disable-line
  //   }

  //   return config
  // },
}
